{
  "name": "emissary",
  "version": "0.1.0",
  "description": "Event bus from dbus to websocket and dbus signal listener that clients can registr to",
  "main": "index.js",
  "author": "Sean Toner",
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.5.2",
    "babel-preset-flow": "^6.23.0",
    "esdoc": "^0.5.2",
    "express": "^4.15.3",
    "flow-bin": "^0.47.0"
  },
  "scripts": {
    "build": "babel src/ -d lib/",
    "prepublish": "yarn run build"
  },
  "dependencies": {
    "dbus-native": "^0.2.1",
    "immutable": "^3.8.1",
    "rxjs": "^5.4.1",
    "ws": "^3.0.0"
  }
}
